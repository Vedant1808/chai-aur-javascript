// 1. How js run some program with example with different phases
// -> Global Execution context
// -> Memory Phase : Variables and functions are stored in memory (hoisting). No code is executed yet.
// -> Execution Phase : Code runs line by line, using values in memory.
// Repeast step 2-3 continuosly for each function

// 2. Call Stack : Keeps track of which function is currently running.Functions are pushed onto the stack when called, and popped off when done.
